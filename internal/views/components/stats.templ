package components

import "fmt"

templ Stats(iconClass string, count int, label string) {
	<div class="item stat">
		<p class="bold">{ label }</p>
		<i class={ "status-icon fa-solid " + iconClass }></i>
		<b class="serif">{ fmt.Sprintf("%d", count) }</b>
	</div>
}
